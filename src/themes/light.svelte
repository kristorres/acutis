<script>
    import {css} from "../utils"

    const theme = css`
        body {
            --background: white;
            --font: Inter;
            --text-size-default: 16px;
            --foreground: #212529;
        }
    `
</script>

{@html theme}
